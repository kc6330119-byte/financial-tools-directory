dist/
venv/
.env
__pycache__/
.DS_Store
*.pyc

# Content (blog posts managed via Airtable for builds)
content/

# Airtable exports and pipeline working files
Advisors Table-Grid view.csv
Advisors-Grid view.csv
Advisors_NEW_ONLY.csv
Advisors_DUPLICATES_SKIPPED.csv
validation_results.csv
validation_flagged.csv
outscraper_advisors*.xlsx

# AdSense remediation working artifacts (protected_urls.txt is NOT ignored — it's a build input)
FACT_DESCRIPTIONS_SAMPLE.md
WEBSITE_DESCRIPTIONS_SAMPLE.md
data/description_backup_*.json
# Phase 2 crawl + LLM response cache (offline working artifact, not a build input)
data/site_cache/
# Raw GSC performance exports — input for extract_protected_urls.py; only the
# derived protected_urls.txt is committed.
data/Pages.csv
*Performance-on-Search*

# Project plans
ADSENSE_RESUBMISSION_PLAN.md
CLAUDE_PLAN_THAT_LEVERAGES_SENIOR_HOME_CARE.md
SENIOR_HOME_CARE_REDESIGN_PROMPT.md

# Cross-site reference material (splashpadlocator playbook + exports — not part of this site)
DGL Files/
